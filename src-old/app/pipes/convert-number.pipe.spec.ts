import { ConvertNumberPipe } from './convert-number.pipe';

describe('ConvertNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
